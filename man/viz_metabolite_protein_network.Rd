% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VizMetaboliteProteinNetwork.R
\name{viz_metabolite_protein_network}
\alias{viz_metabolite_protein_network}
\title{Visualize metabolite-protein networks from MetalinksDB}
\usage{
viz_metabolite_protein_network(
  feature_metadata,
  metalinks_df,
  metabolite_col = NULL,
  hmdb_col = "hmdb",
  save_plot = NULL,
  path = NULL,
  plot_name = "metalinks_network",
  width = 10,
  height = 8,
  hmdb_sep = ";",
  return_data = TRUE,
  print_plot = TRUE,
  full_labels = FALSE,
  label_mode = c("reduced", "all"),
  label_max_chars = 20,
  label_repel = TRUE,
  label_degree_min = 2
)
}
\arguments{
\item{feature_metadata}{Data frame containing at least an HMDB identifier
column and, optionally, a metabolite-name column.}

\item{metalinks_df}{Data frame returned by \code{\link{metsigdb_metalinks}} or any
subset of it. The input must contain at least the columns \code{hmdb} and
\code{gene_symbol}. If present, the columns \code{metabolite}, \code{protein_type},
\code{protein_type_clean}, \code{transport_direction}, \code{type},
\code{mode_of_regulation}, \code{interaction_family}, and \code{source} are used to
enrich the network output.}

\item{metabolite_col}{\emph{Optional: } Single column name in
\code{feature_metadata} containing metabolite labels. If \code{NULL}, normalized
HMDB identifiers are used as metabolite labels. \strong{Default = NULL}}

\item{hmdb_col}{Single column name in \code{feature_metadata} containing HMDB
identifiers. \strong{Default = "hmdb"}}

\item{save_plot}{\emph{Optional: } File type(s) used to save the network
plot. Options are \code{svg}, \code{pdf}, \code{png}, or \code{NULL}.
\strong{Default = NULL}}

\item{path}{\emph{Optional: } Output directory for saved plots.
\strong{Default = NULL}}

\item{plot_name}{\emph{Optional: } Title of the plot and basename for saved
plot files. \strong{Default = "metalinks_network"}}

\item{width}{\emph{Optional: } Plot width in inches for saved plots.
\strong{Default = 10}}

\item{height}{\emph{Optional: } Plot height in inches for saved plots.
\strong{Default = 8}}


\item{hmdb_sep}{\emph{Optional: } Separator used for multiple HMDB IDs in a
single cell of \code{feature_metadata[[hmdb_col]]}. \strong{Default = ";"}}

\item{return_data}{\emph{Optional: } If \code{TRUE}, return a list containing the
plot object and intermediate network tables. If \code{FALSE}, return the plot
invisibly. \strong{Default = TRUE}}

\item{print_plot}{\emph{Optional: } If \code{TRUE}, print the plot.
\strong{Default = TRUE}}

\item{full_labels}{\emph{Optional: } Backward-compatible shortcut for
\code{label_mode = "all"}. If \code{TRUE}, all node labels are shown regardless of
degree. \strong{Default = FALSE}}

\item{label_mode}{\emph{Optional: } Labeling mode for network nodes.
\code{"reduced"} only labels nodes with degree at least \code{label_degree_min};
\code{"all"} labels all nodes. \strong{Default = "reduced"}}

\item{label_max_chars}{\emph{Optional: } Maximum label width before truncation
with \code{...}. \strong{Default = 20}}

\item{label_repel}{\emph{Optional: } If \code{TRUE}, use repelled text labels.
\strong{Default = TRUE}}

\item{label_degree_min}{\emph{Optional: } Minimum node degree required for
labeling when \code{label_mode = "reduced"}. \strong{Default = 2}}
}
\value{
If \code{return_data = TRUE}, a list with the following elements:
\describe{
\item{plot}{The \code{ggraph}/\code{ggplot2} network plot, or \code{NULL} if no network
could be built.}
\item{edges}{A data frame of network edges.}
\item{nodes}{A data frame of network nodes.}
\item{matched_features}{Expanded and normalized feature-to-HMDB mappings
that matched the Metalinks table.}
\item{unmatched_features}{Expanded and normalized feature-to-HMDB mappings
without a Metalinks match.}
\item{saved_files}{Character vector of written plot files.}
}
If \code{return_data = FALSE}, the plot is returned invisibly.
}
\description{
Plot a directed metabolite-protein interaction network based on the tables
returned by \code{\link{metsigdb_metalinks}}. The function also accepts pre-filtered
subsets of the MetalinksDB interaction table and subsets of metabolite
metadata.

HMDB identifiers in \code{feature_metadata} and \code{metalinks_df} are normalized
before matching. Metabolite names are encouraged but not required. If
\code{metabolite_col} is omitted, or if some metabolite names are missing, the
corresponding normalized HMDB identifiers are used as metabolite labels.
}
\examples{\dontrun{
metalinks_transporters <- metsigdb_metalinks(
    cell_location = c("Extracellular"),
    tissue_location = c("Kidney", "All Tissues"),
    biospecimen_location = c("Blood", "Urine")
) |>
    dplyr::filter(stringr::str_detect(tolower(protein_type_clean), "transporter")) |>
    dplyr::distinct(hmdb, .keep_all = TRUE) |>
    dplyr::slice_head(n = 10)

feature_metadata <- metalinks_transporters |>
    dplyr::transmute(
        Metabolite = dplyr::coalesce(.data$metabolite, .data$hmdb),
        hmdb = .data$hmdb
    )

viz_metabolite_protein_network(
    feature_metadata = feature_metadata,
    metalinks_df = metalinks_transporters,
    metabolite_col = "Metabolite",
    hmdb_col = "hmdb",
    save_plot = NULL,
    print_plot = FALSE
)
}}
