% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VizGraph.R
\name{viz_graph}
\alias{viz_graph}
\title{Graph visualization for clustered terms}
\usage{
viz_graph(
  similarity_matrix,
  clusters,
  plot_threshold = 0.5,
  plot_name = "ClusterGraph",
  max_nodes = NULL,
  min_degree = NULL,
  node_sizes = NULL,
  show_density = FALSE,
  seed = NULL,
  save_plot = "svg",
  print_plot = TRUE,
  path = NULL,
  plot_width = 3000,
  plot_height = 2000,
  plot_unit = "px"
)
}
\arguments{
\item{similarity_matrix}{Square numeric matrix of term similarity values.
Row/column names must match \code{clusters} names.}

\item{clusters}{Named vector of cluster labels for each term (e.g., "cluster1").}

\item{plot_threshold}{Similarity threshold used to define edges. Values below the
threshold are removed. Default = 0.5.}

\item{plot_name}{\emph{Optional: } String added to output files of the plot.
Default = "ClusterGraph".}

\item{max_nodes}{\emph{Optional: } Maximum nodes for plotting. If set,
keeps nodes from the largest component up to this limit (by degree).}

\item{min_degree}{\emph{Optional: } Minimum degree filter for graph plotting.}

\item{node_sizes}{\emph{Optional: } Named numeric vector of node sizes,
with names matching term IDs. Values are scaled for plotting.}

\item{show_density}{\emph{Optional: } If TRUE, add a hull background per
cluster. Default = FALSE.}

\item{seed}{\emph{Optional: } Random seed for graph layout reproducibility.
Default = NULL.}

\item{save_plot}{\emph{Optional: } Select the file type of output plots.
Options are svg, pdf, png or NULL. \strong{Default = "svg"}}

\item{print_plot}{\emph{Optional: } If TRUE prints an overview of resulting
plots. \strong{Default = TRUE}}

\item{path}{{Optional:} String which is added to the resulting folder name.
\strong{default: NULL}}
}
\value{
Graph plot as a ggplot object.
}
\description{
Graph visualization for clustered terms
}
\examples{

# Load example data
d <- metsigdb_kegg()

# Run clustering with graph plotting
r <- cluster_pk(
    d,
    metadata_info = c(
        metabolite_column = "MetaboliteID",
        pathway_column = "term"
    ),
    input_format = "long",
    similarity = "jaccard",
    threshold = 0.2,
    clust = "community",
    min = 2,
    plot_name = "GraphExample_long_format",
    save_plot = "png",
    min_degree = 1,
    seed = 123,
    show_density = FALSE,
    max_nodes = 1000
) 

# run graph plotting separately from clustering output but without node_sizes
viz_graph(
    r$similarity_matrix,
    r$clusters,
    plot_threshold = 0.5,
    plot_name = "ClusterGraph",
    max_nodes = NULL,
    min_degree = NULL,
    node_sizes = NULL,
    show_density = TRUE,
    seed = 123,
    save_plot = NULL,
    print_plot = FALSE,
    path = NULL
)

}
